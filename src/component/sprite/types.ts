/**
 * Channel types for multi-texture rendering.
 * Each channel represents a different aspect of the sprite's visual appearance.
 */
export type ChannelType = 'albedo' | 'normal' | 'material' | 'emission';
