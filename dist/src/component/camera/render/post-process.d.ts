import { ViewportT } from '../../viewport';
import { CameraT } from '../data';
export declare function renderUpscale(camera: CameraT, viewport: ViewportT, gl: WebGL2RenderingContext, subPixelOffset?: {
    remainderX: number;
    remainderY: number;
}): void;
//# sourceMappingURL=post-process.d.ts.map