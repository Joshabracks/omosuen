import type { AudioPlayerT } from './data';
import type { AudioTrackT } from '../audio-track/data';
import type { AudioEffectT } from '../audio-effect/data';
export declare class TrackController {
    private _audioPlayer;
    private _track;
    private _sourceId;
    private _repeat;
    private _pitchShift;
    private _speedShift;
    private _volume;
    private _pan;
    private _mix;
    private _spatial;
    private _spatialX;
    private _spatialY;
    private _spatialZ;
    private _reverb;
    private _transitionBuffer;
    private _crossfadeTimer;
    _pauseOffset: number;
    constructor(audioPlayer: AudioPlayerT, track: AudioTrackT, effect?: AudioEffectT, repeat?: boolean);
    private _getActiveSource;
    private _crossfadeParam;
    get track(): AudioTrackT;
    get repeat(): boolean;
    set repeat(value: boolean);
    get isPlaying(): boolean;
    get pitchShift(): number;
    set pitchShift(v: number);
    get speedShift(): number;
    set speedShift(v: number);
    get volume(): number;
    set volume(v: number);
    get pan(): number;
    set pan(v: number);
    get mix(): number[];
    set mix(arr: number[]);
    setMixBand(index: number, value: number): void;
    get spatial(): boolean;
    set spatial(v: boolean);
    get spatialX(): number;
    set spatialX(v: number);
    get spatialY(): number;
    set spatialY(v: number);
    get spatialZ(): number;
    set spatialZ(v: number);
    setSpatialPosition(x: number, y: number, z: number): void;
    get reverb(): number;
    set reverb(v: number);
    get transitionBuffer(): number;
    trackLength(): number;
    trackPosition(): number;
    setTrackPosition(position: number): void;
    play(): number;
    pause(): void;
    stop(): void;
}
//# sourceMappingURL=track-controller.d.ts.map